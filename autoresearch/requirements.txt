# This recipe uses uv (https://docs.astral.sh/uv/) and pyproject.toml + uv.lock
# as the source of truth for dependencies. requirements.txt is here only to
# satisfy the cookbook layout convention.
#
# To install locally for analysis:
#   pip install uv
#   uv sync
#
# Or, if you'd rather use plain pip:
#   pip install "torch==2.9.1" --index-url https://download.pytorch.org/whl/cu128
#   pip install "kernels>=0.11.7" "matplotlib>=3.10.8" "numpy>=2.2.6" \
#               "pandas>=2.3.3" "pyarrow>=21.0.0" "requests>=2.32.0" \
#               "rustbpe>=0.1.0" "tiktoken>=0.11.0"
uv>=0.5.0
